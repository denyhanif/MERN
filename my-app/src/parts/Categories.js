import React from 'react'

export default function Categories({data}) {//{data} artiny hanya menerima tau props saja bukan (preops.data)
    return (
        <div>
            
        </div>
    )
}
